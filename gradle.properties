# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# Local HTTP proxy (port 10808)
systemProp.http.proxyHost=127.0.0.1
systemProp.http.proxyPort=10808
systemProp.https.proxyHost=127.0.0.1
systemProp.https.proxyPort=10808

# MC 26.2+ ships unobfuscated jars (official names); disable loom's mapping pipeline
fabric.loom.disableObfuscation=true

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=26.2
loader_version=0.19.3
loom_version=1.17-SNAPSHOT

# Mod Properties
mod_version=1.0.0-beta-1
maven_group=com.mikitellurium
archives_base_name=superflatbiomeextension

# Dependencies
fabric_api_version=0.156.0+26.2
modmenu_version=20.0.1
yeetus_experimentus_version=102.0.0